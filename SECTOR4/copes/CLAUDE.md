# CoPES — Claude Session Context
## Core Operating and Processing Engine Substrate
### Jerry Leftwich (@jwl247)

## WHAT COPES IS
CoPES is the substrate — the kernel layer Phoenix DevOps OS runs on top of.
NOT a game engine. NOT an application framework. The foundation everything depends on.

## WHAT EXISTS ON DISK
CoPES/src/
  frank.py, helix.py, helix_memory.py
  distro_handler.py, package_handler.py, watcher.py
  security/
    guardian_rotator.py, guardian_base.py
    guardian_alpha.py (auth/sector1)
    guardian_beta.py (process/sector2)
    guardian_gamma.py (network+file/sector3)
    guardian_delta.py (vault+integrity/sector4)
    honeypot.py, copes_runtime.py

CoPES/intake/
  intake.sh, intake.sh.pre_tav

## NEVER
- Translate inside breach_coms drives (translator fires OUTPUT ONLY)
- GPU-dependent solutions (blacklisted)
- Delete from breach_coms4
- Push security stack to public GitHub without Jerry's go-ahead
- Swap Header QR with Footer QR
- Put anything in repo unless tested and pro+ status

## DEPLOYMENT
WSL = sandbox. CoPES machine = real target.
Goal: bootstrap.sh installs clean on cold Ubuntu.
