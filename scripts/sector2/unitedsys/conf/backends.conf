# UnitedSys backend config
# auto-detected by translator.sh — override here if needed
PREFERRED_BACKEND=auto
FALLBACK_BACKEND=none
TRAFFIC_THRESHOLD=10
CATALOG_DB=~/.catalog/catalog.db
LOG_DIR=~/.unitedsys/logs
