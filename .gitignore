__pycache__/
*.pyc
*.pyo

# Virtual envs and build artifacts
.venv/
venv/
node_modules/
dist/
build/
*.egg-info/

# Databases
*.db
*.db-wal
*.db-shm

# Logs
*.log

# Phoenix runtime data - live on the machine, not in the repo
clonepool/
qr/
*.sidecar.json
breach_coms*/
pkg-cache/

# Helix archives
sector1/helix/c_express/*.zip
sector1/helix/lts/*.zip
sector1/kernel/build/
sector1/kernel/dist/
sector1/kernel/venv/
sector1/kernel/phoenix_kernel.spec
tools/benchmarks/*.zip

# Cloudflare
.wrangler/
.env

# Claude Code local settings/state - personal to this machine, never committed
.claude/settings.local.json

# Seelen UI binaries - install via: winget install Seelen.SeelenUI
sector1/kernel/seelen/seelen-static/*.exe
sector1/kernel/seelen/seelen-static/*.dll
sector1/kernel/seelen/seelen-static/uninstall.exe

# Android build
sector2/apps/lifefirst-android/.gradle/
sector2/apps/lifefirst-android/build/
sector2/apps/lifefirst-android/app/build/
sector2/apps/lifefirst-android/local.properties

# .NET build artifacts (hud/ only — repo root bin/ is real source, don't broaden this)
hud/bin/
hud/obj/

# OS artifacts
.DS_Store
Thumbs.db
desktop.ini

# Dev VM home-directory dotfiles — accidental syncs from multipass/SSH sessions,
# not Phoenix source. Never intentional; block re-adding anywhere in the repo.
.bash_history
.bash_logout
.bashrc
.lesshst
.profile
.sudo_as_admin_successful
.gitconfig
.ssh/
**/.cache/motd.legal-displayed

# Credential files
SECTOR4/heix/api drive.txt
SECTOR4/heix/*.txt
**/api drive*

# PoC large binaries — intaked into clonepool, not stored in git
tools/poc/*.qcow2
tools/poc/*.img
tools/poc/*.exe
tools/poc/*.sidecar.json

# Backup blobs at repo root — too large for git history, belong in R2/local only
/dashboardzip1.zip

# USB boot key material — generated by sector1/grub/scripts/gen_key.sh, never committed
sector1/grub/keys/
